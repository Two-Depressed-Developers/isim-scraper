from typing import Optional, List


async def scrape_university_websites(first_name: str, last_name: str, institution: Optional[str] = None) -> List[dict]:
    results = []
    full_name = f"{first_name} {last_name}"
    
    return results
